import{_ as s,c as i,o as a,V as e}from"./chunks/framework.yBzoR3F1.js";const g=JSON.parse('{"title":"JS 模块导入的不同方式和区别","description":"","frontmatter":{"title":"JS 模块导入的不同方式和区别","date":"2023-3-1","category":"前端","tags":["javascript"]},"headers":[],"relativePath":"pages/posts/import-require.md","filePath":"pages/posts/import-require.md"}'),p={name:"pages/posts/import-require.md"},t=e(`<h1 id="js-模块导入的不同方式和区别" tabindex="-1">JS 模块导入的不同方式和区别 <a class="header-anchor" href="#js-模块导入的不同方式和区别" aria-label="Permalink to &quot;JS 模块导入的不同方式和区别&quot;">​</a></h1><p>JavaScript 中，模块是一种可重用的代码块，它将一些代码打包成一个单独的单元，并且可以在其他代码中进行导入和使用。在导入模块时，JavaScript 中有两种常用的方式：<code>import</code> 和 <code>require</code>。</p><p><code>import</code> 是 ES6 引入的新特性，它允许你以声明的方式导入其他模块中的内容。<code>require</code> 是 Node.js 中的特性，它允许你使用一个函数来加载和导入其他模块。</p><h2 id="导入方式" tabindex="-1">导入方式 <a class="header-anchor" href="#导入方式" aria-label="Permalink to &quot;导入方式&quot;">​</a></h2><p>ESM 导入的方式是使用关键字 <code>import</code> 加上大括号（如果需要导入的内容是命名导出的话），再加上模块名的方式进行导入。例如：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> { func1, func2 } </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">from</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;./myModule&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span></code></pre></div><p>CommonJS 导入的方式是使用 <code>require</code> 函数，将需要导入的模块路径作为参数传递给该函数。例如：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> myModule</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./myModule&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><h2 id="文件类型" tabindex="-1">文件类型 <a class="header-anchor" href="#文件类型" aria-label="Permalink to &quot;文件类型&quot;">​</a></h2><p><code>import</code> 只能导入 ES6 模块或者使用 Babel 等工具转化为 ES6 模块的代码。而 <code>require</code> 则可以导入 CommonJS 模块、AMD 模块、UMD 模块以及 Node.js 内置模块等多种类型的模块。</p><h2 id="变量提升" tabindex="-1">变量提升 <a class="header-anchor" href="#变量提升" aria-label="Permalink to &quot;变量提升&quot;">​</a></h2><p>在 ES6 中，<code>import</code> 语句是静态执行的，意味着它们在模块内部的顶层执行，并且在模块内部创建一个局部作用域。这意味着导入的变量只在模块内部可见，并且不会影响模块外部的变量。因此，使用 <code>import</code> 导入的变量是不会被提升的。</p><p><code>require</code> 函数是动态执行的，这意味着它在运行时执行，并且不会在模块内部创建一个局部作用域。因此，使用 <code>require</code> 导入的变量是可以被提升的。</p><details class="details custom-block"><summary>如何理解 import 语句是静态执行的和 require 函数是动态执行的？</summary><p>理解这两个概念需要先了解它们的含义。</p><p>静态执行是指在编译阶段就能够确定其执行结果的代码执行方式。在 JavaScript 中，<code>import</code> 语句属于静态执行的代码，也就是说，当 JavaScript 引擎执行代码时，会在编译阶段对 <code>import</code> 语句进行静态分析，确定所导入的模块，并在运行时加载这些模块。</p><p>动态执行是指在运行时才能确定其执行结果的代码执行方式。在 JavaScript 中，<code>require</code> 函数属于动态执行的代码，也就是说，当 JavaScript 引擎执行代码时，会在运行时动态地确定所需的模块，并加载这些模块。</p><p>因此，可以理解为：</p><ul><li><code>import</code> 语句是静态执行的，因为在编译阶段就能够确定所导入的模块，从而在运行时快速加载这些模块。</li><li><code>require</code> 函数是动态执行的，因为在运行时才能确定所需的模块，需要动态地加载这些模块。</li></ul><p>值得注意的是，由于 <code>import</code> 语句是静态执行的，因此在代码中不能使用变量或表达式作为模块路径，而只能使用字符串字面量。而 <code>require</code> 函数则可以接受变量或表达式作为模块路径，从而动态地确定所需的模块。</p></details><h2 id="导出方式" tabindex="-1">导出方式 <a class="header-anchor" href="#导出方式" aria-label="Permalink to &quot;导出方式&quot;">​</a></h2><p><code>import</code> 和 <code>requrie</code> 在导出方式上也有一些区别。<code>import</code> 使用 ES6 的导出方式，可以使用命名导出和默认导出两种方式进行导出。例如：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 命名导出</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> function</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> func1</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">() {}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 默认导出</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> default</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {}</span></span></code></pre></div><p>而 <code>require</code> 使用 CommonJS 的导出方式，只能使用默认导出方式进行导出。例如：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 默认导出</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exports</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {};</span></span></code></pre></div><p><code>require</code> 除了可以使用 <code>module.exports</code> 导出模块，还可以使用 <code>exports</code> 对象。实际上，<code>exports</code> 对象是 <code>module.exports</code> 的一个引用。当使用 <code>exports</code> 导出时，实际上是向 <code>module.exports</code> 对象添加属性和方法。</p><h2 id="模块作用域" tabindex="-1">模块作用域 <a class="header-anchor" href="#模块作用域" aria-label="Permalink to &quot;模块作用域&quot;">​</a></h2><p>在 JavaScript 中，每个模块都有自己的作用域，模块之间的变量是相互隔离的，不会互相干扰。这也是模块化编程的一个主要特点。</p><p>在使用 <code>import</code> 导入模块时，实际上是在模块内部创建了一个指向被导入模块的引用，而不是直接复制模块中的变量。因此，当不同的文件中使用 <code>import</code> 导入相同的模块时，它们实际上是共享了同一个模块实例，所以可以访问和修改同一个模块中的变量。</p><p>而在使用 <code>require</code> 导入模块时，实际上是将导入模块中的变量直接复制到（可以理解为浅拷贝）当前模块的作用域中。因此，当不同的文件中使用 <code>require</code> 导入相同的模块时，它们实际上是拥有各自独立的模块实例，彼此之间不会共享模块中的变量。</p><p>需要注意的是，如果使用 <code>require</code> 导入的模块中含有可变状态的对象，那么在不同文件中修改该对象的变量会相互影响。这也是 <code>require</code> 在某些情况下会产生一些难以预测的副作用的原因之一。而使用 <code>import</code> 导入的模块，由于是共享同一个模块实例，相对来说更容易管理和控制。</p><p>如果使用 <code>require</code> 导入的模块中含有可变状态的对象，比如一个对象的属性值可以被修改，那么当在不同的文件中修改这个对象中的变量时，由于 <code>require</code> 会将导入的模块中的变量直接复制到当前模块的作用域中，因此会导致这个对象的变量在不同文件中的值相互影响。</p><p>举个例子，假设有一个 <code>config.js</code> 模块，其中定义了一个可变的对象 <code>config</code>，并且在 <code>main.js</code> 和 <code>app.js</code> 两个文件中都使用 <code>require</code> 导入了该模块：</p><div class="vp-code-group vp-adaptive-theme"><div class="tabs"><input type="radio" name="group-2WXLg" id="tab-ir0xb1J" checked="checked"><label for="tab-ir0xb1J">config.js</label><input type="radio" name="group-2WXLg" id="tab-w-gTgJY"><label for="tab-w-gTgJY">main.js</label><input type="radio" name="group-2WXLg" id="tab-PQuf-3Y"><label for="tab-PQuf-3Y">app.js</label></div><div class="blocks"><div class="language-js vp-adaptive-theme active"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> config </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  env: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;dev&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  port: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exports</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> config;</span></span></code></pre></div><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> config</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./config.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 修改config对象的属性值</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config.port </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 4000</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`config.port in main.js: \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">port</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> config</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> require</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;./config.js&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">console.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">log</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">\`config.port in app.js: \${</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">config</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">.</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">port</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">}\`</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span></code></pre></div></div></div><p>当执行 <code>main.js</code> 和 <code>app.js</code> 时，它们都会通过 <code>require</code> 导入 <code>config.js</code> 模块，并且 <code>main.js</code> 中修改了 <code>config</code> 对象的 <code>port</code> 属性值。那么当 <code>app.js</code> 输出 <code>config.port</code> 时，它实际上输出的是被 <code>main.js</code> 修改后的 <code>port</code> 属性值，而不是 <code>config.js</code> 模块原本定义的值。这就是因为 <code>require</code> 导入的模块中还有可变状态的对象或变量，在不同文件中修改该对象或变量会相互影响的原因。</p><p>需要注意的是，这种影响是由于模块之间共享同一个对象的引用造成的，而不是模块本身的问题。因此，在编写模块时，需要谨慎地处理可变状态的对象，尽量避免在不同模块之间共享同一个对象的引用，以避免出现不可预测的副作用。</p><p>为了避免使用 <code>require</code> 导入的模块中含有可变状态的对象或变量，在不同文件中修改对象或变量会相互影响的副作用，有以下几种方法：</p><ul><li><p>使用 <code>import</code> 代替 <code>require</code>：使用 <code>import</code> 导入模块时，不同文件导入同一个模块实际上是共享了同一个模块实例，因此可以避免使用 <code>require</code> 时出现的副作用。</p></li><li><p>使用纯函数：纯函数是指输入相同的参数，输出结果也相同，并且不会对外部环境产生任何副作用的函数。如果在模块中使用纯函数，那么即使该模块中的变量被修改了，但由于纯函数不会产生副作用，因此在不同文件中调用该函数时，输出结果也不会发生变化。</p></li><li><p>使用常量或不可变对象：常量或不可变对象值的是一旦定义后就无法再被修改的值。如果在模块中使用常量或不可变对象，那么即使该模块中的变量被修改了，但由于常量或不可变对象无法修改，因此在不同文件中调用该变量时，其值也不会发生变化。</p></li><li><p>使用模块的副本：可以通过在模块导出时返回一个副本，而不是直接返回模块内部的对象或变量。这样在不同文件中使用 <code>require</code> 导入该模块时，它们会得到不同的副本，而不是共享同一个模块实例。例如：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// config.js</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">let</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> config </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  env: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&#39;dev&#39;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  port: </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">3000</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">.</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">exports</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> =</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> Object.</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">assign</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">({}, config);</span></span></code></pre></div><p>在导出时使用 <code>Object.assign</code> 方法返回 <code>config</code> 对象的一个副本，从而避免了不同文件之间共享同一个模块实例的副作用。</p></li></ul><p>需要注意的是，这些方法并不是万无一失的，而是根据具体情况选用合适的方法来避免副作用。在编写模块时，需要考虑模块中的变量是否需要共享，是否可以被修改，以及模块在不同文件中被调用时可能产生的副作用等因素，从而选择合适的方法来避免副作用。</p><h2 id="import-函数" tabindex="-1">import() 函数 <a class="header-anchor" href="#import-函数" aria-label="Permalink to &quot;import() 函数&quot;">​</a></h2><p><code>import</code> 和 <code>import()</code> 函数是 ES6 新增的两种语法，它们都用于在 JavaScript 中导入模块。虽然它们的作用相同，但它们的语法和用法有所不同。</p><p><code>import()</code> 是 <a href="https://github.com/tc39/proposal-dynamic-import" target="_blank" rel="noreferrer">ES2020 提案</a>中引入的，是用于在动态环境中导入模块的函数，它在运行时执行，而不是在代码加载阶段执行。该函数的语法如下：</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">import</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(moduleName)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">then</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">module</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 使用模块中的内容</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  })</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  .</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">catch</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">((</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">error</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    // 处理错误</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">  });</span></span></code></pre></div><p><code>import()</code> 函数返回一个 Promise，可以在 Promise 的 <code>then</code> 方法中使用导入的模块。与 <code>import</code> 不同，<code>import()</code> 可以在运行时根据需要动态地加载模块，而不需要在代码加载阶段就加载所有模块。</p><p>每个文件中调用 <code>import()</code> 函数时，都会常见一个新的 Promise，这个 Promise 表示将要加载并解析对应的模块，因此每个文件都会获得一个不同的模块实例。但是，实际上整个应用程序只有一个 <code>module.js</code> 模块的实例。</p><p>这是因为，当第一个文件调用 <code>import()</code> 函数时，会开始加载并解析 <code>module.js</code> 模块，当第二个文件调用 <code>import()</code> 函数时，由于该模块已经被加载并解析过了，因此不会重新加载并解析，而是直接返回已经加载并解析好的模块实例。这也意味着，如果在应用程序中多次调用 <code>import()</code> 函数来导入同一个模块，只有第一次调用会执行真正的加载和解析操作，后续调用都会直接返回缓存的模块实例。</p><p>需要注意的是，由于 <code>import()</code> 是异步的，因此在模块加载完成之前，模块中导出的变量或函数是无法使用的。因此，在使用 <code>import()</code> 导入模块时，需要使用 Promise 或 async/await 等机制来处理异步操作。</p><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h2><ul><li><p><code>import</code> 和 <code>import()</code> 都是 ES6 中用于导入模块的语句，而 <code>require()</code> 则是 Node.js 中用于导入模块的函数。</p></li><li><p>使用 <code>import</code> 语句导入模块时，模块会被静态加载，也就是在编译时就已经确定了导入的模块；<code>import()</code> 和 <code>require()</code> 都是动态加载模块的方式，它们都允许在代码运行时根据需要加载模块，而不是在编译时就将所有模块都加载进来。<code>import()</code> 是基于 Promise 的异步加载，<code>require()</code> 是同步加载模块。</p></li><li><p>在整个应用程序中，使用 <code>import</code> 和 <code>import()</code> 语句导入的模块都是单例模式，也就是共用同一个模块实例，而使用 <code>require()</code> 导入的模块会因为赋值而产生多个实例。</p></li><li><p><code>import</code> 和 <code>import()</code> 语句支持模块的默认导出和命名导出，而 <code>require()</code> 只支持模块的默认导出（<code>module.exports</code>）。</p></li></ul>`,43),n=[t];function l(o,h,d,c,k,r){return a(),i("div",null,n)}const y=s(p,[["render",l]]);export{g as __pageData,y as default};
